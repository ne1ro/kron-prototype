// Generated by CoffeeScript 1.6.2
(function() {
  var app;

  app = angular.module("Directives", []);

  app.directive("event", function() {
    return function(scope, element, attrs) {
      $(element).children('.icon-edit').click(function() {
        if ($('.eventbox#edit').is(':hidden')) {
          if ($('.eventbox#new').is(':visible')) {
            $('.eventbox#new').slideUp(500);
          }
          return $('.eventbox#edit').slideDown(500);
        }
      });
      $(element).mouseover(function() {
        if ($(element).children('.description').html() !== "") {
          return $(element).children('p.description').slideDown(500);
        }
      });
      return $(element).mouseleave(function() {
        return $(element).children('.description').slideUp(500);
      });
    };
  });

  app.directive("timeFilter", function(dateFilter) {
    return {
      require: "ngModel",
      link: function(scope, element, attr, ctrl) {
        var format;

        format = attr.timeFilter;
        ctrl.$parsers.push(function(data) {
          return data;
        });
        return ctrl.$formatters.push(function(data) {
          return dateFilter(data, format);
        });
      }
    };
  });

  app.directive("dateTimePicker", function() {
    return function(scope, element, attrs) {};
  });

}).call(this);
