// Generated by CoffeeScript 1.6.2
(function() {
  this.app = angular.module("Kron", ["ngResource", "Filters", "Directives"]);

  $(document).on('ready page:load', function() {
    return angular.bootstrap(document, ['Kron']);
  });

  app.factory("Event", [
    "$resource", function($resource) {
      return $resource("/events/:id", {
        id: '@id'
      }, {
        update: {
          method: "PUT"
        }
      });
    }
  ]);

  app.factory("Calendar", function() {
    var wDay;

    return wDay = null;
  });

  app.factory("socket", function($rootScope) {
    var socket;

    return socket = io.connect('http://localhost:8000');
  });

}).call(this);
